{
  "name": "blog",
  "version": "1.2.0",
  "description": "My Blog",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_OPTIONS=$(node -e \"console.log(Number(process.versions.node.split('.')[0]) >= 17 ? '--openssl-legacy-provider' : '')\") vuepress dev docs",
    "build": "NODE_OPTIONS=$(node -e \"console.log(Number(process.versions.node.split('.')[0]) >= 17 ? '--openssl-legacy-provider' : '')\") vuepress build docs",
    "publish": "./deploy.sh"
  },
  "author": "mybells",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "core-js": "2.6.12",
    "element-ui": "^2.15.13",
    "markdown-it": "^13.0.1",
    "vuepress": "1.9.9"
  },
  "devDependencies": {
    "@vuepress/plugin-back-to-top": "1.9.9",
    "@vuepress/plugin-nprogress": "1.9.9"
  }
}
